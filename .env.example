# LINE Developers > Messaging API channel
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=

# One LINE Official Account should represent one workspace.
LINE_WORKSPACE_MODE=retail

# OpenAI Responses API (server-side only; never expose this key to the browser)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
